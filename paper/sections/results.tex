\section{Results}\label{sec:results}

The evaluation of the self-supervised learning approach is divided into two parts: linear probing results and fine-tuning results.
The linear probing results are intended to give an indirect measure of the quality of the learned image representations of the pre-trained encoder in isolation (sans any segmentation heads or fine-tuning).
As shown in Figure~\ref{fig:probing_metrics}, using fixed embeddings from a ResNet-101 model pre-trained using BYOL as input to a linear classifier yielded substantial improvements in macro F1 score over a classifier that uses embeddings from a model pre-trained only on ImageNet.
The smallest training dataset size of \(N_\text{train} = 250\) exhibited the largest improvement in macro F1 score, with the BYOL pre-trained model achieving a macro F1 score of \num{52.50}\% compared to \num{33.17}\% for the ImageNet pre-trained model, an improvement of \num{19.32}\%.
The embedding model where BYOL pre-training was initialized with ImageNet weights performs marginally worse than the model that begins BYOL pre-training with random weights: the average increase in macro F1 score over the ImageNet pre-trained model was \num{15.44}\% for the BYOL model with random initialization, compared to \num{14.87}\% for the BYOL model with ImageNet initialization.

\input{figures/probing_metrics.tex}

% These trends shifted somewhat when the pre-trained image encoders were used as the backbone in semantic segmentation architectures that were the fine-tuned end-to-end.
While the linear probing results show vast improvements in representation quality when using BYOL pre-training, fine-tuning these pre-trained encoders using various semantic segmentation architectures resulted in different performance trends.
Figure~\ref{fig:finetune_metrics} shows that while the BYOL models that were first initialized with ImageNet weights prior to BYOL pre-training outperformed ImageNet models with no additional pre-training, the BYOL models that were first initialized with random weights have more variable performance depending on the specifics of the semantic segmentation architecture.
The BYOL encoder with random initialization outperformed the ImageNet pre-trained encoder in terms of mean macro F1 score across the UPerNet and DeepLabv3+ models at \(N_\text{train} = 250\), but lagged behind all other architectures at \(N_\text{train} = 500\) and \(N_\text{train} = 750\).
Overall, the ImageNet pre-trained models had a mean macro F1 score of \num{67.34}\%, while the BYOL model with random initialization had a mean macro F1 score of \num{65.61}\%.
However, the BYOL model initialized with ImageNet weights prior to pre-training consistently outperformed the ImageNet pre-trained model across all architectures and training dataset sizes, achieving a mean macro F1 score of \num{69.04}\%.

Increasing the training dataset size generally led to improvements in macro F1 score across all architectures and pre-training strategies, though the magnitude of these improvements diminished as the dataset size increased.
For example, the average increase in macro F1 score when increasing the training dataset size from \(N_\text{train} = 250\) to \(N_\text{train} = 500\) was \num{2.37}\%, while the average increase from \(N_\text{train} = 500\) to \(N_\text{train} = 750\) was only \num{0.69}\%.
The impact of increasing training dataset size is most pronounced for the ImageNet pre-trained models, which saw a mean increase of \num{3.25}\% in macro F1 score when increasing the training dataset size from \(N_\text{train} = 250\) to \(N_\text{train} = 750\), compared to only \num{2.08}\% for the BYOL (ImageNet initialization) models and \num{2.37}\% for the BYOL (random initialization) models.
In terms of semantic segmentation architecture, the U-Net models had the highest mean macro F1 score across all training dataset sizes and pre-training strategies (71.20\%), followed by Attention U-Net (70.85\%), UPerNet (69.01\%), DeepLabv3+ (67.96\%), PAN (67.32\%), and FCN (65.22\%).
No correlation between model size (in terms of number of parameters) and macro F1 score was observed (\(r = 0.01\)), indicating that there was no clear relationship between model complexity and performance in this context.

\input{figures/finetune_metrics.tex}

The best-performing pre-training, semantic segmentation architecture, and training dataset size combination based on mean macro F1 score across all k-folds was the BYOL model initialized with ImageNet weights used as the backbone in a U-Net architecture trained with \(N_\text{train} = 750\) samples, which achieved a mean macro F1 score of \num{74.04}\% and a mean overall accuracy of \num{86.43}\%, a \num{1.47}\% and \num{0.32}\% improvement over the U-Net model using the same training dataset size but with an ImageNet pre-trained backbone, respectively.
To illustrate the qualitative benefits of the BYOL pre-training approach, we highlight a particularly strong individual model run. 
Run \#2 of the U-Net model with the BYOL backbone (ImageNet initialization) fine-tuned using \(N_\text{train} = 500\) samples achieved a macro F1 score of \num{75.84}\%.
A qualitative assessment of this model's inference results is shown in Figure~\ref{fig:visual_assessment}, demonstrating that not only did the model outperform its ImageNet-only counterpart quantitatively (an increase of \num{2.06}\% macro F1 score for the same data split), but it also produced land cover maps with sharper object boundaries and fewer classification artifacts.
Samples (a), (b), and (c) in Figure~\ref{fig:visual_assessment} show overall improvements in classification accuracy with additional pre-training across multiple land cover classes, particularly in homogeneous regions such as water bodies and fields (though some misclassifications still occur, such as in (c)).
The importance of object localization is highlighted in samples (d), (e), and (f), where the ImageNet-only model tends to yield acceptable overall classification accuracy, but fails to accurately delineate small features such as roads and buildings, which are better captured by the model with additional BYOL pre-training.
While these examples are illustrative and do not change the overall ranking reported above, they highlight the qualitative improvements of the BYOL pre-training approach, which are particularly important given the \SI{1}{\meter} spatial resolution of the ground truth labels.

\input{figures/visual_assessment.tex}

Despite this specific model's large individual performance, it was an outlier in terms of performance across all k-folds, as the mean macro F1 score and overall accuracy for this model were \num{73.70}\% and \num{86.55}\%, respectively, falling short of the best-performing configuration in terms of mean macro F1 score.
As such, the U-Net model with a BYOL backbone initialized with ImageNet weights and fine-tuned using \(N_\text{train} = 750\) samples was considered the best-performing model overall and was used to generate the final land cover map for the entire state of Mississippi.
The 4 models trained using this best-performing configuration as part of the k-fold cross-validation procedure were subsequently used to generate a final land cover map for the entire state of Mississippi using the model ensembling and test-time augmentation procedure outlined in Section~\ref{sec:methodology:model_ensembling}.
A visualization of the final comprehensive \SI{1}{\meter} resolution land cover map for Mississippi is shown in Figure~\ref{fig:inference_results}.
The confusion matrix from final accuracy assessment for the dataset is shown in Table~\ref{tab:confusion_matrix}, demonstrating that our model yields high accuracy when classifying natural resources (particularly open water and tree canopy/woody vegetation), while struggling land cover types that are more anthropogenically influenced (particularly barren land and impervious surfaces).
%  the open water and tree canopy/woody vegetation classes in particular, but tends to struggle with the barren land and impervious surfaces classes.
Overall, the final product's macro F1 score and overall accuracy were \num{75.58}\% and \num{87.14}\%, respectively, while the mean class-wise producer's accuracy and user's accuracy were \num{75.60}\% and \num{80.18}\%, respectively.
Figure~\ref{fig:lc_comparison} provides a visual comparison of the final land cover product against comparable existing land cover datasets (NLCD, ESRI Annual Land Use Land Cover~\citep{karraGlobalLandUse2021}) over various land cover settings in Mississippi, providing a visual comparison of the increase in spatial detail our \SI{1}{\meter} resolution product provides over existing products with coarser spatial resolutions (\SI{10}{\meter} for ESRI LULC and \SI{30}{\meter} for NLCD).

\input{figures/inference_results.tex}

\input{tables/confusion_matrix.tex}

\input{figures/lc_comparison.tex}

The final land cover product's macro F1 score represents an increase of \num{1.54}\% over the mean performance of the base k-fold models, which can be attributed to the model ensembling and test-time augmentation procedures outlined in Section~\ref{sec:methodology:model_ensembling}.
Table~\ref{tab:final_f1_comparison} provides a class-wise comparison of the F1 scores between the performance of the models in the k-fold cross-validation and their performance when ensembled.
Notably, the impervious surfaces and barren land classes saw dramatic improvements in F1 score after ensembling, with increases of \num{6.46}\% and \num{3.81}\%, respectively.
Other classes saw more modest improvements, while the unclassified class saw a decrease of \num{2.09}\% in F1 score after ensembling.
The use of the model ensemble approximately quadrupled the time needed for inference compared to a single model, highlighting the trade-off between accuracy and computational efficiency when using model ensembling and test-time augmentation.
For our purposes, we deemed the increase in accuracy worth the additional computational cost, which was not prohibitive given the available computational resources, and elected to use the ensembled predictions for the final land cover product.

\input{tables/ensembling.tex}

% An online dashboard to visualize and interact with the final land cover product can be found at \url{https://www.gcerlab.com/projects/msland}, while the final land cover product can be downloaded from \url{https://doi.org/10.5281/zenodo.16762344}.
% The ground truth patches used to train the model can be found at \url{https://doi.org/10.5281/zenodo.15670823}.



% \section{Results}\label{sec:results}

% \subsection{Validation of semantic stratification approach for selecting training samples}\label{sec:results:semantic_stratification}

% The distribution of the two principal components (PCs) derived from the NLCD histograms for the CBW region in Virginia are shown in Figure~\ref{fig:va_pcs}.
% Principal component 1 (PC1) explained \num{66.21}\% of the variance in the data, while principal component 2 (PC2) explained \num{29.11}\% of the variance, for a total of \num{95.32}\% of the variance explained by the first two PCs.
% PC1 appeared to primarily distinguish candidate patches based on their urban land cover content: patches with larger positive values of PC1 tend to be placed near urban or built-up regions.
% On the other hand, PC2 regions tended to correlate with more vegetated areas.
% A qualitative analysis of samples pulled from clusters of PC1 and PC2 are also demonstrated in Figure~\ref{fig:va_pcs}, demonstrating that samples within the same cluster/stratum tended to have similar land cover characteristics.
% For example, samples in cluster 50 had a high degree of urban land cover, while samples in cluster 210 had a mix of forested and agricultural land cover.
% This homogeneity was not observed across all clusters, however.
% For example, most samples in cluster 122 contained a mix of water and forested land cover, but one sample contained no water at all.

% \input{figures/va_lc_pcs.tex}

% The mIoU results for models trained using the different sampling strategies and dataset sizes according to the procedure outlined in Section~\ref{sec:methodology:semantic_stratification} are shown in Figure~\ref{fig:stratification_results}.
% The two-way ANOVA test indicated that both the sampling strategy (\(F(2, 81) = 22.26, p < 0.001\)) and training dataset size (\(F(2, 81) = 116.72, p < 0.001\)) have a statistically significant effect on model performance, while the interaction between the two factors is not statistically significant (\(F(4, 81) = 2.15, p = 0.082\)).
% However, the Tukey HSD post-hoc test revealed that the semantic stratification strategy does not yield statistically significant improvements in mIoU at \(N_\text{train} = 250\).
% Still, significant improvements in mIoU over both random sampling and spatially stratified sampling were observed at \(N_\text{train} = 500\) and \(N_\text{train} = 750\).
% The largest improvements in mIoU were observed at \(N_\text{train} = 750\), where our proposed stratification method yields an average mIoU increase of \num{3.11}\% over random sampling and \num{2.50}\% over spatially stratified sampling.
% No significant differences in mIoU were observed between random sampling and spatially stratified sampling at any training dataset size.

% \input{figures/stratification_results.tex}

% \subsection{Evaluation of self-supervised learning approach}\label{sec:results:self_supervised_learning}

% \subsubsection{Linear probing results}

% As shown in Figure~\ref{fig:probing_metrics}, using fixed embeddings from a ResNet-101 model pre-trained using BYOL as input to a linear classifier yielded substantial improvements in mIoU over a classifier that uses embeddings from a model pre-trained only on ImageNet.
% The largest training dataset size of \(N_\text{train} = 750\) exhibited the largest improvement in mIoU, with the BYOL pre-trained model achieving an mIoU of \num{42.71}\% compared to \num{27.70}\% for the ImageNet pre-trained model, an improvement of \num{15.01}\%.
% The embedding model where BYOL pre-training was initialized with ImageNet weights performs marginally worse than the model that begins BYOL pre-training with random weights: the average increase in mIoU over the ImageNet pre-trained model was \num{13.90}\% for the BYOL model with random initialization, compared to \num{13.04}\% for the BYOL model with ImageNet initialization.

% \input{figures/probing_metrics.tex}

% \subsubsection{Fine-tuning results}

% These trends shifted somewhat when the pre-trained image encoders were used as the backbone in semantic segmentation architectures that were the fine-tuned end-to-end.
% Figure~\ref{fig:finetune_metrics} show that while the BYOL models that were first initialized with ImageNet weights prior to BYOL pre-training outperformed ImageNet models with no additional pre-training, the BYOL models that were first initialized with random weights have more variable performance depending on the specifics of the semantic segmentation architecture.
% The BYOL encoder with random initialization only outperformed the ImageNet pre-trained encoder in terms of mean mIoU across all architectures except U-Net and Attention U-Net at \(N_\text{train} = 250\), but only DANet, PAN, and PSPnet at \(N_\text{train} = 500\), and only DANet and PSPNet at \(N_\text{train} = 750\).
% However, the BYOL model initialized with ImageNet weights prior to pre-training consistently outperformed the ImageNet pre-trained model across all architectures and training dataset sizes, echoing the results observed in the linear probing experiment (though to a lesser degree).

% \input{figures/finetune_metrics.tex}

% The best-performing pre-training, semantic segmentation architecture, and training dataset size combination based on mean mIoU across all k-folds was the BYOL model initialized with ImageNet weights used as the backbone in a U-Net architecture trained with \(N_\text{train} = 750\) samples, which achieved a mean mIoU of \num{60.86}\% and a mean overall accuracy of \num{86.43}\%, a \num{1.88}\% and a \num{0.32}\% improvement over the U-Net model using the same training dataset size but with an ImageNet pre-trained backbone, respectively.
% % The best-performing individual model (without averaging across k-folds) was run \#2 of the U-Net model the BYOL backbone with ImageNet initialization fine-tuned using \(N_\text{train} = 500\) samples, achieving an mIoU of \num{63.01}\% and an overall accuracy of \num{87.42}\%.
% % This was an increase of \num{2.77}\% and \num{1.32}\% over a similar model with a backbone pre-trained only on ImageNet that was provided the exact same training/validation split.
% % A qualitative assessment of the inference results from this model is shown in Figure~\ref{fig:visual_assessment}, demonstrating that not only did the model outperform the ImageNet pre-trained model quantitatively, but also produced land cover maps with sharper object boundaries and fewer classification artifacts, corresponding more closely to the true land cover

% % The best-performing pre-training, semantic segmentation architecture, and training dataset size combination based on mean mIoU across all k-folds was the BYOL model initialized with ImageNet weights used as the backbone in a U-Net architecture trained with \(N_\text{train} = 750\) samples, which achieved a mean mIoU of \num{60.86}\% and a mean overall accuracy of \num{86.43}\%, a \num{1.88}\% and a \num{0.32}\% improvement over the U-Net model using the same training dataset size but with an ImageNet pre-trained backbone, respectively.
% To illustrate the qualitative benefits of the BYOL pre-training approach, we highlight a particularly strong individual model run. 
% Run \#2 of the U-Net model with the BYOL backbone (ImageNet initialization) fine-tuned using \(N_\text{train} = 500\) samples achieved an mIoU of \num{63.01}\%.
% A qualitative assessment of this model's inference results is shown in Figure~\ref{fig:visual_assessment}, demonstrating that not only did the model outperform its ImageNet-only counterpart quantitatively (an increase of \num{2.77}\% mIoU for the same data split), but it also produced land cover maps with sharper object boundaries and fewer classification artifacts.

% \input{figures/visual_assessment.tex}

% Despite this specific model's large individual performance, this model was an outlier in terms of performance across all k-folds, as the mean mIoU and overall accuracy across all k-folds for this model was \num{60.36}\% and \num{86.55}\%, respectively, falling short of the best-performing configuration in terms of mean mIoU.
% As such, the U-Net model with a BYOL backbone initialized with ImageNet weights and fine-tuned using \(N_\text{train} = 750\) samples was considered the best-performing model overall and was used to generate the final land cover map for the entire state of Mississippi.
% The k-fold models from this best-performing configuration were then ensembled and used to generate a final land cover map for the entire state of Mississippi using the procedure outlined in Section~\ref{sec:methodology:model_ensembling}.
% The confusion matrix for the final thematic accuracy assessment for the statewide land cover map is shown in Table~\ref{tab:confusion_matrix}.
% Overall, the final product's mIoU was \num{62.77}\% and overall accuracy was \num{87.14}\%, while the mean class-wise producer's accuracy and user's accuracy were \num{75.60}\% and \num{80.18}\%, respectively.
% This represents an increase of \num{1.91}\% in mIoU over the mean performance of the base k-fold models, which can be attributed to the model ensembling and test-time augmentation procedures outlined in Section~\ref{sec:methodology:model_ensembling}.
% % This increase of \num{2.41}\% and \num{0.59}\% in mIoU and overall accuracy, respectively, can be attributed to the model ensembling and test-time augmentation procedures outlined in Section~\ref{sec:methodology:model_ensembling}.
% An online dashboard to visualize and interact with the final land cover product can be found at \url{https://www.gcerlab.com/projects/msland}, while the final land cover product can be downloaded from \url{https://doi.org/10.5281/zenodo.16762344}.
% The ground truth patches used to train the model can be found at \url{https://doi.org/10.5281/zenodo.15670823}.

% \input{tables/confusion_matrix.tex}