\section{Conclusion}

% \note{The conclusion has been heavily edited by removing all sentences that referred to the semantic stratification method. The remaining sentences have been reassembled to form a new, concise conclusion that summarizes only the findings related to self-supervised learning.}
In this work, we have demonstrated that accurate land cover classification at VHSR can be achieved using as few as 1,000 256\(\times\)256-pixel patches of labeled data by utilizing a BYOL self-supervised pre-training objective to learn meaningful image representations from large amounts of unlabeled VHSR imagery prior to fully supervised fine-tuning.
We found that initializing a model with ImageNet weights and then continuing pre-training with BYOL was consistently the best-performing approach, suggesting that there is still value in utilizing models pre-trained on general-purpose imagery for remote sensing applications, even if there is a demonstrable domain gap.
Still, pre-training a model using BYOL from a random initialization was often competitive with ImageNet pre-training alone, demonstrating the power of in-domain self-supervision and the need for remote sensing-specific pre-training, even beyond the context of foundation models.
We used this approach to produce a \SI{1}{\meter} resolution land cover map for the entire state of Mississippi, USA, which captures fine-scale land cover features that are indistinguishable in existing land cover products such as the NLCD.
This achievement serves as a blueprint for both researchers applying geospatial deep learning models to data-scarce scenarios as well as practitioners seeking to produce high-quality land cover maps at VHSR over major geographic extents with limited human resources for collecting ground truth data.

% Given the rapid pace at which deep learning and semantic segmentation models have become the standard for classifying land cover at VHSR, it is critical to investigate their implications for real-world applications where labeled data is often scarce and expensive to obtain.
% Future work should continue to explore the implications of self-supervised learning for remote sensing applications under real-world constraints beyond academic benchmarks and compute-intensive techniques.
% Given the growing interest around self-supervised learning and foundation models in remote sensing, we believe our findings on the effectiveness of a self-distillation approach for pre-training a region-specific image encoder for land cover classification under data-scarce scenarios demonstrates the utility of these approaches beyond the context of building massive foundation models.
% Our work demonstrates that smaller-scale, domain-specific, and architecture-agnostic self-supervision is a viable strategy for improving model performance even for more traditional model architectures and workflows.


% \section{Conclusion}

% In this work, we have demonstrated that accurate land cover classification at VHSR can be achieved using as few as \num{1,000} labeled training patches when employing a combination of machine-learning driven semantic stratification to select informative training samples and self-supervised learning to pre-train a deep convolutional image encoder on large amounts of unlabeled VHSR imagery.
% Our proposed semantic stratification approach leverages existing land cover maps at coarse resolution to create strata based on the land cover composition of candidate training patches, ensuring that the training dataset is well-representative of the land cover characteristics of the target area.
% This approach can yield statistically significant improvements in land cover classification accuracy compared to simple random sampling and spatially stratified sampling.
% Further, we have demonstrated how self-distillation based self-supervised learning can be used to learn meaningful image representations from large amounts of unlabeled VHSR imagery which are more relevant to the target land cover classification task than those produced by models pre-trained on ImageNet, which is the current standard practice in remote sensing applications.
% When fine-tuning an entire segmentation model, we found that pre-training on ImageNet and then continuing pre-training with BYOL consistently outperformed using ImageNet pre-training alone.
% Furthermore, pre-training with BYOL from a random initialization was often competitive with the standard ImageNet-only approach, demonstrating the power of in-domain self-supervision.
% % We applied this methodology to produce a \SI{1}{\meter} resolution land cover map for the entire state of Mississippi...
% % Additionally, fine-tuning these BYOL pre-trained image encoders using a small number of labeled training as part of an end-to-end trainable semantic segmentation framework (e.g., U-Net) on few samples also reveals moderate competitive performance compared to models pre-trained on ImageNet, while using a model pre-trained on ImageNet then BYOL prior to fine-tuning outperforms ImageNet pre-training without the need for any additional labeled data.
% % We applied this methodology to produce a \SI{1}{\meter} resolution land cover map for the entire state of Mississippi, USA, which captures fine-scale land cover features that are indistinguishable in existing land cover products such as the NLCD.

% % \note{FINISH ME!!!}
% We applied this methodology to produce a \SI{1}{\meter} resolution land cover map for the entire state of Mississippi, USA, which captures fine-scale land cover features that are indistinguishable in existing land cover products such as the NLCD.
% This achievement serves as a blueprint for both researchers applying geospatial deep learning models to data-scarce scenarios as well as practitioners seeking to produce high-quality land cover maps at VHSR over major geographic extents with limited human resources for collecting ground truth data.
% Given the rapid pace at which deep learning and semantic segmentation models have become the standard for classifying land cover at VHSR, it is critical that we investigate the implications of these models to real-world applications where labeled data is often scarce and expensive to obtain.
% Specifically, the lack of literature documenting the best practices for selecting patch-based training samples for deep learning semantic segmentation highlights the major gap between research and practice in this area, and we aim to bridge this gap with our proposed semantic stratification approach.
% Future work should explore the generalizability of our methodology to other geographic regions and remote sensing tasks: we hypothesize that our stratification approach can be easily adopted for other land cover classification tasks at VHSR and improved further by incorporating additional sources of information (e.g., topography, climate, etc.) to define strata.
% Finally, we believe our findings on self-supervised learning contribute to the burgeoning interest around foundation models in remote sensing.
% While large-scale foundation models are promising, our work demonstrates that smaller-scale, domain-specific, and architecture-agnostic self-supervision are a viable strategy for improving model performance even for more traditional model architectures and workflows.
% We also believe that our findings on the effectiveness of self-supervised learning for pre-training image encoders for remote sensing-specific tasks with limited labeled data corroborate growing sentiment around the future of foundation models in remote sensing, while also demonstrating how self-supervised learning can still be useful even outside of the context of foundation models.