\section{Methodology}\label{sec:methodology}

% The proposed methodology consits of several main components: (1) the evaluation of our proposed ``semantic stratification'' routine for improved sample selection using data from the CPB land use/land cover dataset, (2) curation and visual annotation of a limited land cover dataset using samples selected according to the porposed strategy applied over the state of Mississippi, (3) pre-training a deep ResNet-101 image encoder using BYOL, (4) fine-tuning of a semantic segmentation architecture with the pre-trained backbone using the limited ground truth data, and (5) producing and validating a large-scale \SI{1}{\meter} resolution land cover map over the state of Mississippi.
Our proposed methodology consists of a multi-stage approach to land cover classification that aims to produce a land cover product at high accuracy under data-scarce conditions using a machine-learning driven stratification protocol that aims to extract informative samples for training and the use of self-supervised learning to pre-train a large vision model for feature extraction from color-infrared aerial imagery in the absence of ground truth data.
First, we apply the semantic stratification approach to train and evaluate land cover classification model using a subset of the CPB land use/land cover dataset in Virginia in order to validate the approach in the context of other stratification approaches.
We then use this approach to select 1000 sample tiles over the state of Mississippi for manual visual annotation to build a training dataset.
A seperate, larger dataset of unannotated sample tiles from the state of Mississippi will be used to pre-train a ResNet-101 convolutional encoder using  