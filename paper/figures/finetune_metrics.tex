\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/finetuning_results.pdf}
    \caption{mIOU performance of semantic segmentation architectures using backbones pre-trained with ImageNet only (green), BYOL only (orange), and ImageNet + BYOL (blue) given various amounts of labeled training data \note{INCREASE FONT SIZE AND FIX BBOX CLIPPING IN BOTTOM RIGHT}.}\label{fig:finetune_metrics}
\end{figure}